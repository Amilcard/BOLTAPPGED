# ── Dépendances & build ──
node_modules/
.next/
tsconfig.tsbuildinfo

# ── Environnement ──
.env
.env*.local
.env.production
.vercel

# ── OS / éditeurs ──
.DS_Store
.vscode/
*.log

# ── Artifacts locaux dev ──
_untracked_backup/
prisma/dev.db
*.bak
fix-typing-booking-flow.patch

# ── Archives & audits locaux ──
_archives/
*.xlsx
.~lock.*
app/debug-db/

# ── n8n workflows (contiennent des tokens/credentials) ──
n8n-patches/*.json
n8n-workflows/
01_UFOVAL_IS_FULL_SESSIONS_v4.json

# ── Tests & caches ──
.jest-cache/
test-results/
playwright-report/
coverage/
.claude/agent-memory/

# ── Fichiers temporaires ──
deploy-fixes.patch
generate_clean_logo.js
supabase_functions_search_path_fix.sql
supabase_security_fix.sql
FICHIERS_CREES.txt
stay.ageMax

# ── Documents légaux ──
Documents_Legaux/

# ── Contenu éditorial Flooow (hors scope GED_APP) ──
social/

# tooling IA local
.ai-workspace/
.claude/agent-memory/
.claude/settings.local.json
.claude/launch.json
.claude/skills/
.claude/worktrees/
.mcp.json

# Supabase CLI temp (auto-généré, user-local)
supabase/.temp/

# Rapports tests locaux
test-reports/

# Briefs contenant potentiellement des secrets (à conserver hors repo)
BRIEF_SUPABASE_KEYS_*.md
BRIEF_*_KEYS_*.md

# Fichiers de génération images IA (workflow local)
GENERATION_IMAGES_*.json
GENERATION\ IMAGES*.json

# Exports Excel anciens formats
*.xltx

# fichiers temporaires
.tmp-check.ts

# build output & DB locale
out/
prisma/dev.db

# ── Règles agents IA ──
.agent/rules/

# ── Rapports dependency-cruiser (générés localement) ──
deps.svg
deps-archi.svg
deps.dot
deps-archi.dot
deps.html
deps.json

# Fixtures de test (contient tokens réels)
.env.test

# ── Scripts et patches contenant potentiellement des secrets ──
# Les fichiers n8n exportés contiennent souvent des credentials en dur.
# Ne commiter que des versions nettoyées (placeholders {{...}}).
# install_ged_complete.sh exclut aussi pour éviter de ré-exposer un mot de passe.
