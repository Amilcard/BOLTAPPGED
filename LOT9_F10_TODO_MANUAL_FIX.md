# F10 - Remplacement texte partenaire (FIX MANUEL REQUIS)

**Fichier** : `app/sejour/[id]/stay-detail.tsx`
**Ligne** : ~462
**Statut** : ⚠️ Fichier en deadlock - modification à faire manuellement

## Modification à faire

### Texte actuel (à supprimer)
```html
<span>La connaissance du projet associatif de notre partenaire</span>
```

### Nouveau texte (à mettre)
```html
<span>Faites partie de nos 0 % d'exclusions</span>
```

## Context
Cette modification fait partie de la refonte des textes pour l'application GED Flooow Inklusif, afin de mettre en avant l'engagement d'inclusion sociale (0% d'exclusions).

## Actions
1. Ouvrir `app/sejour/[id]/stay-detail.tsx`
2. Chercher "projet associatif de notre partenaire" (ligne ~462)
3. Remplacer par "Faites partie de nos 0 % d'exclusions"
4. Sauvegarder

**Note** : Le fichier est actuellement en deadlock et ne peut pas être modifié automatiquement.
