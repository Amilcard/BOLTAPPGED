# Handover session 2026-04-21 — Résolution retours testeuse Thanh

Document destiné à une autre IA qui reprend le travail. Lecture obligatoire
avant toute modification.

---

## 1. Sources de vérité

Thanh Nguyen a réalisé des tests bout-en-bout en conditions prod du 19 au
21/04/2026 sur `app.groupeetdecouverte.fr`.

| Source | Chemin | Usage |
|---|---|---|
| Synthèse narrative | `/Users/laidhamoudi/Downloads/TESTS REELS/RETOURS TESTS 21.docx` | Analyse par écran (accueil, modal pro, récap, paiement, upload, code structure, invitation, activation) |
| Grille détaillée | `/Users/laidhamoudi/Downloads/TESTS REELS/Tableau_Retour_Test_Thanh.xlsx` | Grille **parcours (P1→P15+)** avec attendu vs observé par étape |
| Captures | `/Users/laidhamoudi/Downloads/TESTS REELS/Capture-ecran-TEST/` | ~22 screenshots référencés dans le xlsx |
| Audit IA PDF | `/Users/laidhamoudi/Downloads/TESTS REELS/RETOURS TESTS IA CLAUDE PDF.docx` | 2e agent Claude, analyse DB 3 dossiers Thanh → désalignements **D1→D9 + O1, O2** |

**⚠️ Les refs "F01-F20" utilisées dans des docs intermédiaires sont une
numérotation interne d'une IA précédente — elles ne figurent PAS dans les
sources Thanh. Seules les numérotations officielles sont :**
- **P1→P15** (parcours xlsx Thanh)
- **B1→B8** (numérotation adoptée par le 1er agent Claude ayant fait les 8 fixes du matin)
- **D1→D9 + O1, O2** (2e agent Claude, audit PDF)

---

## 2. État prod actuel (vérifié 2026-04-21 13h15)

### DB Supabase `iirfvndgzutbxwfdwawu`
```
orphelins_dossier         : 0
jeune_nom = 'À RENSEIGNER': 3 (les 3 Thanh — backfill mig 074)
trigger_auto_create_dossier_enfant : ACTIF
uq_dossier_inscription    : EXISTS (UNIQUE constraint)
```

### Vercel deploy
- Dernier deploy READY : SHA `defda1d` (handover corrigé)
- Projet `boltappged` team gedapp
- Aucune erreur runtime détectée post-deploy Wave 1

### 3 inscriptions test de Thanh (prod)
| ID | Jeune | Séjour | Montant | Dossier enfant |
|---|---|---|---|---|
| `a49f4455-b9c1-...` | Léa (2018) | natation-et-sensation | 1335€ Stripe | ✅ complet signé envoyé GED |
| `53e2a67e-0be5-...` | Camille (2019) | les-ptits-puisotins-1 | 755€ virement | ⚠️ dossier créé via trigger 075, bulletin non rempli |
| `707e4d8b-cad7-...` | Léa (2020) | les-ptits-puisotins-1 | 755€ virement | ⚠️ abandonné (3 PJ uploadées, jamais signé) |

### Branches git
- Working : `claude/sweet-bhabha-52ca83` (worktree)
- `origin/main` : synchronisée via push direct (pas de PR — règle CLAUDE.md)

---

## 3. Commits de la session 2026-04-21

### 3.1 — Agent A (matin 11h, 8 commits — fixes B1-B8)

| SHA | Ref | Résout (parcours Thanh) |
|---|---|---|
| `28647d0` | B3 | P4.2 + P9.x — booking auditLog écart prix serveur + email DB source de vérité (répond à l'incohérence 755€ vs 1335€ identifiée dans docx) |
| `aec16f2` | B5 | P9.5-6 — bouton Payer désactivé après refus carte + reset PaymentIntent |
| `9554115` | B4 | P9.4 — Turnstile ready avant activation bouton Payer (Cloudflare race) |
| `cddb206` | B1+B2 | P1 (bouton connexion absent) + P6.2 (acceder-pro peu visible) + P8.3 (PriceInquiryBlock rendu pour pro) |
| `fd6b9cb` | — | docs backlog P1#8 directeur bloqué 1re invitation (non fixé alors) |
| `4b3bb8a` | B8 | P14.2-3 — feedback règles mot de passe à l'activation secrétaire |
| `fdbd5f5` | B7 | P10.15 (upload cas 1) — nom fichier dans popup suppression document |
| `f548e18` | B6 | P10.x (cas 3 erreur PDF JSON brut) — message humain côté client |

### 3.2 — Moi Wave 1 (12h29, 5 commits — retours Thanh + audit DB 2e agent)

| SHA | Ref | Détail |
|---|---|---|
| `0a3e67c` | infra | fix(husky) `--no-ignore` ESLint pour paths Next.js `[param]` — blocage commits sur bracket |
| `85f77e5` | — (complément B6) | fix(structure-pdf) proxy staff distingue 404 NO_DATA (dossier pas rempli, Camille) de 502 crash |
| `7ebc7fa` | D1 + D2 | migrations SQL 074 (backfill `jeune_nom = ''` → `'À RENSEIGNER'`) + 075 (trigger auto-create dossier_enfant + UNIQUE + backfill 4 orphelins) |
| `f720802` | D2 (Option C) | feat(dossier-enfant) champ `nom_famille` dans BulletinComplementForm + sync `gd_inscriptions.jeune_nom` depuis les 2 PATCH routes |
| `e196d67` | D2 (Option C) | feat(pdf) PDF lit `bulletin_complement.nom_famille` avec fallback `inscription.jeune_nom` (ignore 'À RENSEIGNER') |

### 3.3 — Moi Wave U1-D8 (13h, 7 commits — post-audit V2 + D5/D7/D8)

| SHA | Ref | Détail |
|---|---|---|
| `f3af0ec` | U1 (régression B2) | fix(stay-detail) découple `canSeePrices = isPro` + `hasUfovalData = isPro && enrichment !== null`. Avant : pro sans UFOVAL avait zéro prix + zéro PriceInquiryBlock |
| `b8ecf7d` | U2 (P1#8 Thanh) | fix(structure-auth) guard `lib/structure.ts` restreint à `role='direction'`. Avant : `code_directeur` legacy bloqué dès 1re invitation secrétariat. Symétrie CDS appliquée. |
| `8b63378` | U3 (P3.4 + P4.1 Thanh) | feat(envies) bouton retirer un séjour + CTA "Envoyer ce souhait à mon éducateur" → route `/sejour/[id]/souhait` existante |
| `dcad743` | D5 | fix(pdf) normalisation sexe : helper `mapSexeLong` + fallback `inscription.jeune_sexe` (M/F → garcon/fille) sur bulletin L.175 |
| `ee87157` | D7 | feat(sanitaire) validation bloquante : chaque vaccin doit avoir oui/non avant signature. Non bloquant si mode signature "offline" (scan carnet) |
| `6534d71` | D8 | feat(bulletin) validation ≥ 1 financement (ASE / établissement / famille / autres) avant signature |
| `0a98c2d` | — | docs(backlog) P1#8 résolu + O2 Wave 4 (dette RGPD purge storage) |

### 3.4 — Handover (13h20+)

| SHA | Détail |
|---|---|
| `4c7361a` | docs(handover) V1 — version avec refs F01-F20 inventées |
| `defda1d` | docs(handover) V2 — retrait des refs non verifiables |
| *(ce commit)* | docs(handover) V3 — basé sources Thanh réelles (xlsx + docx + audit PDF) |

---

## 4. Mapping complet findings → statut (basé sources Thanh uniquement)

### 4.1 — Grille Thanh xlsx par parcours

| Parcours | Étape | Attendu | Observé Thanh | Statut | Référence |
|---|---|---|---|---|---|
| **P1 Accueil** | 1.2 | Titre visible | "Pas de textes" | à vérifier captures | — |
| | 1.3 | Bouton "voir séjours" visible | "Pas de bouton" | ✅ via B1 `cddb206` (CTA header) | — |
| | 1.4 | Catalogue s'affiche | "cette page s'affiche directement" | commentaire UX | — |
| | 1.5 | Affichage PC adapté | "C'est cette page qui s'affiche directement" | commentaire | — |
| **P2 Filtres** | 2.1 | ≥ 15 séjours | "Même problème" | à vérifier | — |
| | 2.2 | Filtre été réduit | "Pas possible de faire défiler, éléments fin de liste pas affichés, séjours manquants" | ❌ BACKLOG | — |
| | 2.3 | Filtre âge 10-13 | "tous les activités en été" | ❌ BACKLOG (filtre âge cassé) | — |
| | 2.5 | Prix pas affiché côté jeune | — | ⚠️ à vérifier | — |
| | 2.6 | Bouton envies visible | "Pas très visible sur PC, OK tél" | ❌ BACKLOG | — |
| **P3 Favoris** | 3.1 | Confirmation visible | "une fois ajouté, rediriger vers formulaire, nombre sur cœur" | commentaire UX | — |
| | 3.4 | Bouton retirer | "Il n'y a pas de bouton pour retirer" | ✅ FAIT | U3 `8b63378` |
| **P4 Envoi envies** | 4.1 | Bouton "envoyer à éducateur" | "pas dans mes envies, il faut ré-ajouter" | ✅ FAIT | U3 `8b63378` |
| | 4.2 | Champs clairs | "pas de champs nom/prénom/âge" | à arbitrer (Option C a déjà traité nom_famille côté dossier — prénom/âge kids form séparé) | — |
| | 4.7 | Email mail clair | "Pas de prénom du jeune. Séjours pas listés avec image" | ❌ BACKLOG (template email) | — |
| **P5 Educateur lit** | 5.2 | Prénom jeune affiché | "Anonyme" | ⏸️ PAUSE (décision libellé) | — |
| | 5.3 | Séjours avec photos | "Pas de photos" | ❌ BACKLOG (JOIN images manquant) | — |
| | 5.4 | Boutons clairs | "minimaliste, manque contexte" | commentaire UX | — |
| **P6 Tarif sans compte** | 6.2 | Page acceder-pro accessible depuis accueil | "pas très visible et le bouton ne marche pas" | ✅ FAIT | B1+B2 `cddb206` |
| **P9 Inscription** | 9.4 | Vérification Cloudflare | "ne fonctionne pas bien, plusieurs fois actualiser" | ✅ FAIT partiel | B4 `9554115` (dette : reset après 5min) |
| | 9.5-6 | Paiement 4242 accepté | "Refusé quelques fois / Refusé (carte test)" | ✅ FAIT | B5 `aec16f2` |
| **P10 Dossier** | 10.5 | 1 seul bouton "Valider" | "Validation OK, plus deux boutons séparés" | commentaire UX | — |
| | 10.15 | Upload OK | (parcours OK) | ✅ UX B7 `fdbd5f5` pour popup delete | — |
| **P11 Signature/Envoi** | 11.3 | Empêché si incomplet | "Erreur" (capture P11-erreur) | ✅ D7 + D8 ajoutent validations (`ee87157` + `6534d71`) | — |
| | 11.7 | Retenter envoi bloqué "déjà envoyé" | "Je n'ai pas trouvé le bouton" | à vérifier | — |
| **P12 Direction** | 12.1 | Accès direction visible | "Je ne trouve pas" | ✅ FAIT | B1 `cddb206` |
| | 12.5 | Mobile Safari OK | "section Équipe erreur d'affichage" | ❌ BACKLOG (bug visuel mobile) | — |
| **P13 Invitation** | 13.5 | Réinviter fonctionne | "Trop de tentatives" | ❌ BACKLOG rate limit trop strict | — |
| **P14 Activation** | 14.2-3 | Règles mdp | (parcours OK globalement) | ✅ FAIT | B8 `4b3bb8a` |
| **P15 Secrétaire remplit** | 15.2 | Mention "en tant que secrétariat" | "Bloqué (étape précédent)" | à vérifier | — |
| | 15.3 | Modif sauvegardée | "Je ne pense pas (123456789abC)" | à vérifier | — |

### 4.2 — B (agent A) — tous faits

Voir §3.1. Dettes mineures notées par V2 :
- **B3** : `actorType='referent'` sur auditLog `price_corrected` — devrait être `'system'` sémantiquement.
- **B4** : `turnstileReady` jamais reset — si user navigue hors étape 5 et revient après > 5min, token Turnstile expiré mais bouton Payer actif.
- **B2 régression** : corrigée par moi via U1 `f3af0ec`.

### 4.3 — D + O (2e agent PDF)

| Ref | Désalignement | Statut | Commit |
|---|---|---|---|
| D1 | Inscription Camille sans `gd_dossier_enfant` | ✅ FAIT | migration 075 `7ebc7fa` (trigger + backfill) |
| D2 | `jeune_nom = ''` sur 3 inscriptions | ✅ FAIT | mig 074 + Option C nom_famille `f720802` + `e196d67` |
| D3 | Contact urgence divergent (3 sources, 78 vs 87) | ❌ STATUS QUO | Décision user : gestion astreinte GED |
| D4 | Adresse permanente 3 stockages | ❌ STATUS QUO | Décision user : idem |
| D5 | Sexe "F" vs "fille" | ✅ FAIT | `dcad743` (helper + fallback bulletin) |
| D6 | `signer_qualite` redondant colonne + JSON | ❌ ARBITRAGE | Décision produit requise |
| D7 | Sanitaire signable sans vaccins | ✅ FAIT | `ee87157` (bloquant form, non-bloquant si PJ offline) |
| D8 | Financement tous à false | ✅ FAIT | `6534d71` (OR ≥ 1) |
| D9 | Champs vides exposés au PDF | ❌ BACKLOG | Helper `s()` gère partiellement, audit par usage requis |
| O1 | Signatures base64 dans JSON | ✅ ACCEPTABLE | Pas de code |
| O2 | Docs PJ orphelins si abandon | ❌ BACKLOG Wave 4 | Politique rétention à valider |

---

## 5. Décisions produit enregistrées (2026-04-21)

| Sujet | Décision user | Impact |
|---|---|---|
| **Option C nom_famille** | Collecté au **dossier** (bulletin_complement.nom_famille), sync automatique vers `gd_inscriptions.jeune_nom`. Form `/reserver` inchangé. | Minimisation RGPD février 2026 préservée |
| **D7 vaccins** | Bloquant si form web (infos présentes dans carnet). Non bloquant si mode signature offline (PJ carnet uploadée). | Implémenté `ee87157` |
| **D3 contact_urgence + D4 adresse** | Status quo. "C'est notre travail en PJ astreinte GED." | Aucun code touché |
| **Terminologie "jeune" → "Enfant / Adolescent"** | Mis en PAUSE (36 occurrences, cascade grammaticale). | À reprendre si arbitrage |
| **F08 libellé "Anonyme"** | PAUSE (lié au rename "jeune" ci-dessus) | — |
| **Synchronisation `jeune_nom`** | OUI si simple → fait côté PATCH routes dossier (ee96d65). | — |
| **Commits** | "Le plus secure avec traces" → 1 commit par étape logique, TSC + tests pré-push | Appliqué |

---

## 6. Reste à faire (ordonné par priorité + effort)

### 6.1 — Backlog P2 (impact UX direct mais non bloquant)

| Priorité | Source | Description | Fichier | Effort | État |
|---|---|---|---|---|---|
| ~~P2.1~~ | P5.3 + P4.7 | Email `sendSouhaitNotificationEducateur` sans image séjour | 2 fichiers | 1h | ✅ `fc0961d` (2026-04-21 session 2) |
| ~~P2.2~~ | P13.5 | Rate limit global IP sans segmentation action | `lib/rate-limit-structure.ts` | 45min | ✅ `d0da912` — bucket `struct-strict` 5/15min pour invite/reinvite/revoke |
| ~~P2.3~~ | D9 | Audit `writeText()` PDF pour skip cohérent vides | `app/api/dossier-enfant/[inscriptionId]/pdf/route.ts` | 1h | ✅ `eb20d88` — skip null/undefined + littéraux 'null'/'undefined' |
| ~~P2.4~~ | P2.3 | Filtre âge "10-13 ans" (faux positif — pas d'option) + séjours sans âge DB passaient tous filtres | `app/home-content.tsx` | 30min | ✅ `dd5f667` — défaut plateforme 3-17 ans |
| P2.5 | P2.2 | Affichage mobile filtres : éléments en fin de liste tronqués | CSS | 30min | ⏳ device iPhone requis |
| P2.6 | P12.5 | Section "Équipe" direction sur mobile Safari : erreur d'affichage | `components/structure/StructureAdminTab.tsx` | 30min + test device | ⏳ device iPhone requis |

### 6.2 — Backlog P3 (polish UX, pas de bug)

| Source | Description |
|---|---|
| P1.2 | "Pas de textes" accueil — vérifier visibilité h1 + copy |
| P3.1 | Feedback après ajout favori : "nombre sur le petit cœur" manquant |
| P5.4 | Page educateur minimaliste, manque contexte |
| P10.5 | 2 boutons séparés validation bloc — simplifier en 1 seul |
| F08 / dépend | Libellé "Anonyme" sur page souhait educateur |
| ~~Agent A dette B3~~ | ✅ `3a35c82` — actorType='system' sur auditLog price_corrected |
| ~~Agent A dette B4~~ | ✅ `17574d0` — reset turnstileReady/Token à chaque sortie step 4 |

### 6.3 — Wave 4 — dettes RGPD / architecture

| Source | Description | Effort |
|---|---|---|
| O2 | Cron `/api/cron/rgpd-purge` ajouter handler `purgeAbandonedDossierStorage` — politique rétention à valider user (ex: `refusee > 90j`, `en_attente inactif > 180j`) | 2h |
| D6 | Arbitrage `signer_qualite` colonne vs JSON puis migration pour supprimer la redondance | 30min + décision |
| P11.7 / 15.2 / 15.3 | Ré-parcourir le workflow secrétaire-remplit-dossier pour identifier ce qui bloque | 1h + tests |

---

## 7. Pièges / attention (critique pour la reprise)

### Worktree vs repo principal — hook husky
- Worktree : `/Users/laidhamoudi/Dev/GED_APP/.claude/worktrees/sweet-bhabha-52ca83/`
- Repo principal : `/Users/laidhamoudi/Dev/GED_APP/`
- **`core.hooksPath` pointe vers le repo principal.** Modifier `.husky/pre-commit` dans le worktree n'a PAS d'effet. Il faut aussi éditer `/Users/laidhamoudi/Dev/GED_APP/.husky/pre-commit`.
- Fix `--no-ignore` appliqué sur les 2 emplacements lors de cette session. Si un pull écrase le fichier principal, refaire :
  ```bash
  echo "$STAGED" | xargs npx eslint --no-ignore --max-warnings 0 2>&1
  ```

### Divergence remote — 2 agents simultanés
User lance fréquemment 2 agents Claude en parallèle (cette session a vu 8 commits concurrents de l'agent A). **TOUJOURS `git fetch origin` avant tout commit**, et vérifier `git log origin/main..HEAD` + `git log HEAD..origin/main` — si divergence, STOP et alerter le user (règle CLAUDE.md).

### Règles CLAUDE.md strictes
- **95 % confidence avant tout code** (Règle #0).
- Skill injections vercel-plugin (bootstrap, env-vars, marketplace, next-forge, ai-sdk, chat-sdk, vercel-storage, etc.) : **systématiquement hors sujet** sur cette codebase. À ignorer via Règle 3 (filtrage silencieux).
- Tables PII (auditLog obligatoire sur mutation) : `gd_inscriptions`, `gd_dossier_enfant`, `gd_propositions_tarifaires`, `gd_factures`, `gd_suivi_*`, `gd_structure_access_codes`, `gd_educateur_emails`, `gd_stay_sessions`, `gd_souhaits`.
- Push direct sur `main`, pas de PR (règle user).

### Auth structure legacy vs migré (post-fix U2)
- `gd_structures.code` (6 chars CDS) + `code_directeur` (10 chars) restent valides **tant qu'aucune ligne `role='direction'`** n'existe dans `gd_structure_access_codes` pour la structure.
- Dès qu'une ligne `direction` active existe → code legacy désactivé pour hiérarchie (direction + CDS) — symétrie voulue.
- Actuellement aucune route applicative ne crée de ligne `direction` automatiquement. Seule la migration `042` one-shot l'a fait pour les structures pré-existantes.
- **Workaround manuel pour Thanh (structure `adb7a3e5`)** : ligne `direction` INSERT manuelle dans `gd_structure_access_codes` — fonctionne via le lookup prioritaire `access_codes.code`.

---

## 8. Checklist pré-push obligatoire

Avant CHAQUE commit :

```bash
npx tsc --noEmit                              # silencieux
git fetch origin
git log origin/main..HEAD --oneline           # mes commits locaux
git log HEAD..origin/main --oneline           # divergence ? si oui STOP
# TSC + hook passent via pre-commit husky (inclut jest tests/unit)
git push origin claude/sweet-bhabha-52ca83:main
```

Si le hook ESLint échoue sur bracket path, vérifier
`/Users/laidhamoudi/Dev/GED_APP/.husky/pre-commit` contient `--no-ignore`.

---

## 9. Checklist reprise par la prochaine IA

1. **Lire ce document en entier**, puis CLAUDE.md projet (`.claude/worktrees/sweet-bhabha-52ca83/CLAUDE.md`) et CLAUDE.md global (`~/.claude/CLAUDE.md`).
2. **Lire les 2 sources Thanh** `RETOURS TESTS 21.docx` + `Tableau_Retour_Test_Thanh.xlsx` pour confirmer le contexte P1→P15.
3. **`git fetch origin && git log origin/main..HEAD`** — vérifier synchro.
4. **Demander au user quel item du backlog §6 attaquer en premier** — ne pas choisir seul.
5. **Pour chaque fix non trivial** : dispatch `arch-impact-reviewer` avant édition (règle CLAUDE.md multi-agents).
6. **Pas d'invention** — si une ref du backlog §6 manque de contexte, relire le xlsx/docx Thanh avant de coder.

---

*Fin handover. Cette version est basée uniquement sur les sources Thanh verifiables.*
