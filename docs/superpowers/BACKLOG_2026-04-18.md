# Backlog reprise 2026-04-18

**Source** : session 2026-04-17 → 2026-04-18 — vagues architecte 1+2+3.
**HEAD** : 29b9a6b (+ vague 3 à venir)
**État tests** : 477 PASS / 0 FAIL / 8 skip (pre-commit hook actif)
**tsc** : 0 erreur

---

## Chantiers vague 3 (en cours d'exécution)

- [x] W1 UUID_DEL → UUID_RE (backlog vague 2)
- [x] W2 rgpd-purge auto-trace audit_log
- [x] W3 UX audit écrans session (diagnostic read-only)
- [x] W4 Doc backlog (ce fichier)
- [ ] W5 ESLint global sweep (Phase B séquentielle)
- [ ] W6 Duplicate index gd_stays/gd_stay_sessions (Phase B)
- [ ] W7 RLS auth_rls_initplan wrap — **validation humaine requise** avant apply

## Reporté hors vague 3

### Sprint dédié XL
- [ ] 207 occurrences `{error:'string'}` → `{error:{code,message}}` (35 fichiers, ~2j)
- [ ] E2E Playwright 3 parcours (team-invite, kids, pro proposition→facture)

### Décisions métier pending
- [ ] `cds_delegated` routes direction-only (invite/settings/delegation/revoke/reinvite) — trancher subrogation
- [ ] Password reset flow team members
- [ ] Session revocation UI "Déconnecter tous les membres" (jti insert backend déjà OK)
- [ ] i18n emails team-invite + propositions + factures
- [ ] Dossier enfant workflow complet (fiche sanitaire, liaison, documents)

### Dashboard user (action utilisateur requise)
- [ ] Supabase HIBP "Leaked Password Protection" — toggle dashboard
- [ ] Sentry alertes croissance `gd_revoked_tokens`
- [ ] Appliquer migration 074 (5 index FK) manuellement SQL Editor — `CONCURRENTLY` hors transaction

### Scheduled
- [ ] Retirer fallback legacy `verifyProSession` — **après 2026-05-17** (expiration JWTs 8h pré-Phase 1)
- [ ] Test manuel cron `rgpd-purge` via Vercel Dashboard avant 1er mai 2026

### Requalification pending
- [ ] Migration reads vers vue `gd_structure_members` — 0 consumer admin actuel, trancher utilité

---

## État architecture fin session

### Commits
- 57 commits main (session 2026-04-17→18)
- Branches annexes : aucune (main only)
- Vercel auto-deploy depuis main

### Patterns établis
- Body-based URL pour POST/PATCH/DELETE admin (anti-SSRF Opengrep)
- Helpers partagés `lib/admin-*.ts` entre route legacy et nouvelle route body-based
- `UUID_RE` + `EMAIL_REGEX` centralisés `lib/validators.ts`
- `buildProSessionToken` unique helper JWT pro-session
- `auditLog()` obligatoire sur routes PII (règle #15)
- Guards rate-limit dual IP + identifier (email/token)

### Migrations DB
- 068 team_invitation_columns
- 069 access_codes_role_check
- 070 access_codes_last_jti
- 071 view_gd_structure_members (security_invoker=true)
- 072 cleanup_duplicate_fk_session_prices
- 073 inscription_last_relance_at
- 074 fk_indexes (à appliquer manuellement SQL Editor)

### Rustines actives
- 1 seule pré-existante : `app/api/dossier-enfant/[inscriptionId]/pdf/route.ts:92` (path-traversal whitelist, justifiée mars 2026)

---

## Règles opérationnelles à respecter (reprise)

1. CLAUDE.md "Consultation multi-agents avant intervention" (section ajoutée commit `ac692dc`)
2. Workflow : arch-impact-reviewer brief → worker TDD → cross-review
3. Commits atomiques, push après chaque (branche main only)
4. tsc + jest + pre-commit hook obligatoires
5. Zéro rustine (`nosemgrep`/`codacy-disable`) sauf justification documentée

---

## Contact reprise

- Plan architecte vague 1 : `docs/superpowers/plans/2026-04-17-architect-fixes.md`
- Plan team-invite : `docs/superpowers/plans/2026-04-17-team-invitation-flow.md`
